# Hotmart Webhook → Firestore Dashboard

Endpoint serverless para receber **webhooks da Hotmart**, validar token e **persistir no Firestore**. Dashboard **em tempo real** (HTML único) com filtros e exportação CSV.

## Estrutura
